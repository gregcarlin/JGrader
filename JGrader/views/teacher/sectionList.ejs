<div class="page-header">
  <h1>Sections</h1>
  <a href="/teacher/section/create" class="right plus-button"><span class="glyphicon glyphicon-plus-sign glyph-button" data-toggle="tooltip" data-placement="left" title="New Section"></span></a>
</div>
<div class="table-responsive">
  <table class="table table-striped table-linked">
    <thead>
      <tr>
        <th>Name</th>
        <th>Students</th>
        <th>Current Assignment</th>
      </tr>
    </thead>
    <tbody>
      <% if(rows.length == 0) { %>
        <tr>
          <td colspan="3">You have no sections!</td>
        </tr>
      <% } else {
          for(i in rows) {%>
            <tr onclick="document.location = '/teacher/section/<%= rows[i].id %>'">
              <td><%= rows[i].name %></td>
              <td><%= rows[i].count %></td>
              <td><%- rows[i].aname ? '<a href="/teacher/assignment/' + rows[i].aid + '">' + rows[i].aname + '</a>' : 'None' %></td>
            </tr>
       <% }
         } %>
    </tbody>
  </table>
</div>
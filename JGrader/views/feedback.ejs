<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <meta name="description" content="">
    <meta name="author" content="">
    <!--<link rel="icon" href="../../favicon.ico">-->

    <title>Feedback | JGrader</title>

    <!-- Bootstrap core CSS -->
    <link href="/stylesheets/bootstrap.min.css" rel="stylesheet">

    <!-- Custom styles for this template -->
    <link href="/stylesheets/signin.css" rel="stylesheet">

    <!-- HTML5 shim and Respond.js IE8 support of HTML5 elements and media queries -->
    <!--[if lt IE 9]>
      <script src="https://oss.maxcdn.com/html5shiv/3.7.2/html5shiv.min.js"></script>
      <script src="https://oss.maxcdn.com/respond/1.4.2/respond.min.js"></script>
    <![endif]-->
  </head>

  <body>

    <div class="container">
      <form class="form-signin" role="form" action="/feedback" method="POST">
  		  <h2 class="form-signin-heading">Feedback</h2>
        <% if(typeof error != 'undefined') { %>
          <div class="error"><%= error %></div>
        <% } %>
        <input type="text" class="form-control" name="fname" placeholder="First Name" required autofocus<%- typeof fname == 'undefined' ? '' : ' value="'+fname+'"' %>>
        <input type="text" class="form-control" name="lname" placeholder="Last Name" required<%- typeof lname == 'undefined' ? '' : ' value="'+lname+'"' %>>
      	<input type="email" class="form-control" name="email" placeholder="Email" required<%- typeof email == 'undefined' ? '' : ' value="'+email+'"' %>>
        <textarea class="form-control" rows="4" name="feedback" placeholder="Feedback"></textarea>
        <div class="iama">
          Type of Feedback:
          <label><input type="radio" name="type" value="bug" required<%- (typeof role == 'undefined' || role == 'bug') ? ' checked' : '' %>>Bug</label>
          <label><input type="radio" name="type" value="enhancment"<%- (typeof role != 'undefined' && role == 'enhancment') ? ' checked' : '' %>>Enhancment</label>
        </div>
        <button class="btn btn-lg btn-primary btn-block" type="submit">Submit Feedback</button>
  		</form>
    </div> <!-- /container -->


    <!-- IE10 viewport hack for Surface/desktop Windows 8 bug -->
    <script src="/javascripts/ie10-viewport-bug-workaround.js"></script>
  </body>
</html>
